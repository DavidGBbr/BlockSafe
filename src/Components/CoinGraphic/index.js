import React from "react";
import { View, Text } from "react-native";

export const CoinGraphic = ({ sparkline }) => {
  // console.log(sparkline);
  return (
    <View>
      <Text style={{ fontSize: 26, color: "#fff" }}>
        Gráfico da Criptomoeda
      </Text>
    </View>
  );
};
